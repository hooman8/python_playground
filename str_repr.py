# for repr, it's how would I tell Python to ccreate a new instance of this class
# str value can be thought of as the human readable string value

import datetime

now = datetime.datetime.now()
print(str(now))
print(repr(now))
